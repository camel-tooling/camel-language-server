version: 2.1

orbs:
  maven: circleci/maven@1.0.3

workflows:
  maven_test:
    jobs:
      - maven/test:
         command: 'verify -V'
         matrix:
           parameters:
             executor: [tag: "8.0", tag: "11.0", tag: "15.0"]
